# Datatypes

Padrao Java.

```kt
fun main(args: Array<String>) {

    // === numeros inteiros ===
    var b: Byte = 1;
    var s: Short = 1;
    var i: Int = 1;
    var l: Long = 1L;

    // === numero decimal ===
    var f: Float = 2.2f;
    var d: Double = 2.2; //Mais preciso. Tem mais casas decimais.

    // === String ===:
    var textoInsano: String = "Salve";

    // === Char ===:
    var charInsano: Char = 'A';

    // === Boolean ===:
    var trueOrFalse: Boolean = true;
}
```

<br>

📖 Os datatypes são os mesmos do Java. A diferença é que no Kotlin é `Int`.

<hr>
<br>

## Type Inference

Podemos informar o datatype ou não.

```kt
// Sem especificar o tipo (inferência)
var n1 = 22;
var n2 = 44f;
var n3 = "Salve";
var n4 = false;

// Especificando o tipo
var n1: Int = 22;
var n2: Float = 44f;
var n3: String = "Salve";
var n4: Boolean = false;
```

📖 O compilador do Kotlin é inteligente o suficiente pra olhar pro valor da variable e saber seu datatype.

<br>

- 💡 Quando o número é inteiro é `Int`
- 💡 Quando o número é quebrado é `Double`
- 💡 Quando o número é muito grande é `Long`

<hr>
<br>

## Não usamos primitive datatype no Kotlin

Para o Kotlin tudo é um objeto. No caso dos datatypes primitivos, nós sempre usamos o wrapper classe.

Que seria o datatype primitive transformado para objeto.

<hr>
<br>


## Visualizando números de uma forma melhor

```kt
val n1: Int = 22000;
val n2: Int = 22_000; //mais facil de ler
```

