# companion object

Um `companion object` está vinculado a uma classe, permitindo-nos definir attributes e methods `static` semelhantes aos do Java.

Ou seja, os attributes e methods dentro de um companion objeto **são chamados através da própria classe** e não das suas instancias.

## Syntax

```kotlin
data class Person (
    val name: String,
    val age: Int
){

    companion object{

        val nomeBanco = "ITAU";
        val enderecoBanco = "CEIC ITAU";

        fun toStringNaMao(): String {
            return "$nomeBanco e $enderecoBanco"
        }
    }
}
```

> ⚠️ O `companion object` não consegue acessar os attributes e methods da classe em que está.
>
> Como é chamado de forma estática, nenhuma instância é informada — então não tem como saber a qual objeto esses attributes pertencem.
> 

```kotlin
fun main(args: Array<String>) {

    //chamando de maneira static
    Person.nomeBanco;
    Person.enderecoBanco;
    Person.toStringNaMao();

    //por debaixo dos panos
    Person.Companion.nomeBanco;
    Person.Companion.enderecoBanco;
    Person.Companion.toStringNaMao();
}
```

---

## Nomeando um companion object

O nome default de um companion object é `Companion`.

Se quiser, voce pode alterar o nome desse companion object. Geralmente o pessoal não faz isso, mas é legal saber que existe.


```kotlin
companion object ClasseBolada {

    val nomeBanco = "ITAU";
    val enderecoBanco = "CEIC ITAU";

    fun toStringNaMao(): String {
        return "$nomeBanco e $enderecoBanco"
    }
}
```

```kotlin
fun main(args: Array<String>) {

    //ninguem usa assim...
    Person.ClasseBolada.nomeBanco;
    Person.ClasseBolada.enderecoBanco;
    Person.ClasseBolada.toStringNaMao();
    
    //maneira mais usada
    Person.nomeBanco;
    Person.enderecoBanco;
    Person.toStringNaMao();
}
```

---

## Como saber quantas intancias de uma Class foram criadas?

```kotlin
data class Person (
    val name: String,
    val age: Int
){
    //corpo do constructor primário
    init {
        quantidadePessoasCriadas++;
    }

    companion object {
        var quantidadePessoasCriadas: Int = 0;
    }

}
```

:book: O companion object não consegue acessar os attributes e methods das instancias. Entretanto, as instancias conseguem acessar as informacoes do companion object.

```kotlin
fun main(args: Array<String>) {

    val p1 = Person("a", 123);
    val p2 = Person("a", 123);
    val p3 = Person("a", 123);

    println(Person.quantidadePessoasCriadas);
}
```